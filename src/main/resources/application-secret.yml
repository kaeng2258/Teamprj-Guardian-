spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: test-client-id
  mail:
    password: local-password
custom:
  security:
    oauth2:
      client:
        registration:
          kakao:
            devUser:
              oauthId: test-oauth-id
              nickname: test-nickname

  jwt:
    secretKey: "this_is_a_local_secret_key_for_tests_please_change"

mfds:
  api:
    base-url: "https://apis.data.go.kr/1471000/DrbEasyDrugInfoService"
    service-key: "${MFDS_EASYDRUG_SERVICE_KEY:}"
    connect-timeout-ms: 4000
    read-timeout-ms: 4000

server:
  ssl:
    enabled: true
    key-store: "file:certs/keystore-localhost.p12"
    key-store-password: "changeit"
    key-store-type: "PKCS12"
    key-alias: "localhost"

webpush:
  vapid:
    public-key: "${WEB_PUSH_PUBLIC_KEY:}"
    private-key: "${WEB_PUSH_PRIVATE_KEY:}"
    subject: "${WEB_PUSH_SUBJECT:mailto:support@guardian.com}"
  scheduler:
    timezone: "${WEB_PUSH_TIMEZONE:Asia/Seoul}"
